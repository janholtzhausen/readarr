2026-03-19

Decision 1
- Scope: add download of existing book files from the Readarr UI.
- Rationale: book search/grab already exists; the missing user-facing capability is downloading the managed file itself.

Decision 2
- Conversion path: support optional on-demand conversion through configured Calibre root-folder settings when available.
- Rationale: the codebase already has Calibre conversion support; reusing it is lower-risk than inventing a second conversion stack.

Decision 3
- UI entry point: expose download actions on managed book files, with original download always available and EPUB download shown only when conversion is available.
- Rationale: file-level actions avoid ambiguity for multi-part books and keep the first implementation small enough to verify properly.

Decision 4
- Conversion UX: start with EPUB as the exposed on-demand output.
- Rationale: it directly addresses the reported text-encoding cleanup use case without forcing a larger format-selection UI in the first pass.

Decision 5
- Verification approach: use the built container for live endpoint validation and a focused unit-test fixture for the conversion-capable service logic.
- Rationale: the host lacks native .NET and Yarn toolchains, and live EPUB conversion requires an external Calibre-backed root folder that the disposable test instance does not have.

Decision 6
- Toolbar visibility: surface download actions on the main book toolbar using the first loaded managed file, while retaining per-file actions in the Files tab.
- Rationale: this makes the feature discoverable on the page the user actually visits without removing the more precise file-level controls.

Decision 7
- Conversion backend: add a local `ebook-convert` fallback in the Readarr image, with remote Calibre still preferred when configured.
- Rationale: this makes converted downloads work in a default single-container deployment instead of requiring a separate Calibre server.

Decision 8
- Download format scope: expose both `EPUB` and `AZW3` in the UI, backed by the existing format parameter on the same download endpoint.
- Rationale: both are common reader targets, and adding them now avoids a second UX iteration while keeping the backend surface area unchanged.

Decision 9
- Runtime verification: validate the feature with a real Calibre-generated MOBI fixture and confirm original, `EPUB`, and `AZW3` downloads through the API while checking Readarr debug logs.
- Rationale: this proves the local `ebook-convert` fallback works in the built image rather than only in unit tests.

Decision 10
- UI gating: remove the frontend-only conversion-availability gate from the toolbar and file actions so conversion buttons always render as links when a managed file exists.
- Rationale: the backend already enforces conversion support, and the missing/false client flag was causing dead toolbar buttons on live pages.

Decision 11
- Toolbar rendering: allow `PageToolbarButton` callers to pass a custom CSS class and use that to widen the book-page download buttons.
- Rationale: the live bundle already contains the labels, so the remaining issue is the default toolbar width clipping those three buttons rather than missing text.

Decision 12
- Linked toolbar buttons: make `PageToolbarButton` render link-backed actions as `inline-block` and override the shared link-blue styling.
- Rationale: the blue icons prove the download actions are still picking up generic anchor styling, which prevents them from matching the existing white toolbar buttons and from honoring width as expected.

Decision 13
- Book-page toolbar downloads: switch the top-toolbar download actions from `to=` links to normal `onPress` buttons that open the resolved download URL.
- Rationale: existing toolbar text buttons already render correctly, so using the same button path avoids the remaining anchor-only rendering discrepancy while preserving one-click downloads.

Decision 14
- Icon choice: use built-in Font Awesome Free icons already present in the project, with `book-open` for `EPUB` and `tablet-screen-button` for `AZW3`.
- Rationale: they are clearer than the generic export glyphs and avoid introducing a second icon library or custom SVG asset pipeline.

Decision 15
- React migration target: move Readarr to the Sonarr-aligned React 18 baseline first, not directly to React 19.
- Rationale: Sonarr already proves the same frontend architecture works on React 18.3.1, making it the lowest-risk step before any later React 19 compatibility work.

Decision 16
- React 19 readiness: treat a React 19 upgrade as a dependency-and-legacy-cleanup project, not a direct version bump.
- Rationale: the frontend still contains `findDOMNode` call sites, extensive function-component `defaultProps`, and multiple packages whose published peer ranges stop at React 17 or even React 16.

Decision 17
- `findDOMNode` cleanup: replace Readarr-owned `findDOMNode` usages with explicit DOM refs and stop suppressing the related React warning.
- Rationale: this removes the clearest React 19 deprecation blocker we control directly and makes any remaining third-party `findDOMNode` warnings visible during testing.

Decision 18
- `defaultProps` cleanup scope: prioritize the 62 function components that still use `defaultProps`, especially the 45 under shared `Components/`, before touching the 105 class-component cases.
- Rationale: function-component `defaultProps` are the React-19-relevant warning source, and cleaning shared primitives first removes the most warnings with the fewest edits.

Decision 19
- First `defaultProps` cleanup batch: convert low-risk shared function components to parameter/destructuring defaults instead of `defaultProps`.
- Rationale: these primitives are reused widely, so clearing them first reduces React 19 warning noise quickly while keeping the changes mechanical and easy to verify.

Decision 20
- Verification result for the first `defaultProps` batch: keep the batch and proceed to push only after both frontend and container-level startup checks pass.
- Rationale: the edits are purely UI-prop default handling changes, so a successful webpack build plus a clean disposable container boot and `initialize.json` response is sufficient validation before live redeploy.

Decision 21
- Deployment result for the first `defaultProps` batch: promote the verified image to `192.168.1.5:15000/readarr:latest` and redeploy it on TrueNAS after local curl/log checks pass.
- Rationale: this preserves the required test-then-push flow and confirms the cleanup does not regress production startup or the live EPUB download path.

Decision 22
- Second `defaultProps` cleanup batch: continue with low-risk shared function components in `Components/` that only need parameter defaults and matching `propTypes` relaxations.
- Rationale: this keeps the React 19 readiness work incremental and mechanical while shrinking the remaining function-component `defaultProps` count in the highest-impact shared UI layer.

Decision 23
- `FormLabel` exception: keep `FormLabel.defaultProps` in place for now because removing it breaks TSX call-site inference for optional props like `name` and `hasError`.
- Rationale: React 19 readiness work should not trade away build stability; this one needs either an explicit TypeScript prop type or a broader component conversion before its `defaultProps` can be removed safely.

Decision 24
- Second batch deployment: promote the verified image with the additional shared-component cleanup and confirm it in production with `initialize.json`, a live EPUB download, and post-start logs.
- Rationale: this preserves the same validated path as the first batch and proves the reduced `defaultProps` surface does not regress the running service.

Decision 25
- Third `defaultProps` cleanup batch: focus on JS-only shared primitives and selectors that do not rely on the `FormLabel`-style JSX optional-prop inference path.
- Rationale: this lets the cleanup continue moving forward while avoiding another round of avoidable TypeScript compatibility breakage.

Decision 26
- `TableRow` exception: keep `TableRow.defaultProps` in place for now because TSX row call sites rely on it to make `overlayContent` optional.
- Rationale: this is the same JSX inference constraint seen with `FormLabel`; removing the runtime default without adding explicit TypeScript props breaks otherwise-valid TSX usage.

Decision 27
- Third batch deployment: promote the JS-only shared-component cleanup that avoids the documented TSX-sensitive exceptions, then verify the running production service with `initialize.json`, a live EPUB download, and startup logs.
- Rationale: this keeps React 19 readiness progressing while proving the narrowed batch still deploys cleanly and preserves the active download path.

Decision 28
- Fourth cleanup batch: remove `defaultProps` from small JS feature/presentation components in `Author/` and `Book/` where the defaults are local and do not participate in TSX optional-prop inference.
- Rationale: these are low-risk wins that continue reducing the remaining function-component `defaultProps` count without reopening the TypeScript compatibility issues already identified.

Decision 29
- Fifth cleanup batch: remove `defaultProps` from activity, settings, and modal/menu components where the defaults are simple booleans or labels and the call sites are not TSX-sensitive.
- Rationale: these components reduce the remaining function-component count quickly and are still straightforward to validate through the existing build and production verification path.

Decision 30
- Sixth cleanup batch: remove `defaultProps` from the remaining low-risk activity, settings, modal, menu, and table-cell function components, then redeploy only after production curl and log verification pass.
- Rationale: these defaults are still simple local fallbacks, so they can be converted mechanically while continuing to shrink the React 19 warning surface without touching the known TSX-sensitive exceptions.

Decision 31
- React 19 readiness status after the sixth batch: Readarr now has 12 function-component `defaultProps` remaining and 105 class-component `defaultProps` remaining, with the unresolved function cases concentrated in shared form/table primitives plus the known `FormLabel` and `TableRow` TypeScript exceptions.
- Rationale: recording the exact post-deploy counts and remaining file set keeps the migration state durable and makes the next safe cleanup target obvious.

Decision 32
- Seventh cleanup batch: remove the remaining mechanical function-component `defaultProps` from shared form and table primitives, while intentionally leaving `FormLabel` and `TableRow` untouched.
- Rationale: those two files are the only known TypeScript-sensitive exceptions, so clearing the rest reduces the React 19 warning surface to the minimum app-owned blocker set without destabilizing JSX call-site inference.

Decision 33
- React 19 readiness status after the seventh batch: only 2 function-component `defaultProps` remain in Readarr, specifically `FormLabel` and `TableRow`, while 105 class-component `defaultProps` remain.
- Rationale: the component cleanup is now effectively complete apart from the documented TSX exceptions, so the next real React 19 work is explicit prop typing for those two files and then dependency modernization.

Decision 34
- Final function-component cleanup: convert `FormLabel` and `TableRow` from JS components that relied on `defaultProps` into typed `tsx` components with optional props and parameter defaults.
- Rationale: this preserves JSX optional-prop behavior for TypeScript call sites without keeping `defaultProps`, eliminating the last app-owned function-component React 19 blocker cleanly.

Decision 35
- React 19 readiness status after the typed-component conversion: Readarr now has 0 function-component `defaultProps` remaining and 105 class-component `defaultProps` remaining.
- Rationale: the direct component-level cleanup is complete, so the next gating work for a React 19 move is dependency modernization rather than more `defaultProps` removal.

Decision 36
- React 19 dependency modernization: bump Readarr's frontend runtime to React 19, replace `react-virtualized` usage with `react-window`, and migrate the remaining drag-and-drop code off the legacy React DnD HOC API.
- Rationale: React 19 removed `react-dom/findDOMNode`, and the old `react-virtualized` plus `DragSource`/`DropTarget`/`DragLayer` path was the last concrete build/runtime blocker after the earlier component cleanup.

Decision 37
- React 19 validation and promotion: keep the React 19 build, push `192.168.1.5:15000/readarr:latest` at digest `sha256:7e6f2edcf49212b9c7ae5320f283fcd55b1ce5278c3d40600f2863a8f7a25a91`, and redeploy it to `192.168.1.4` after local container smoke tests and live production curl/log checks pass.
- Rationale: the frontend build succeeded, the disposable container served `initialize.json`, and the live host now serves `initialize.json` at `10.0.0.38214`, returns `200 OK` for the EPUB download endpoint, and shows a clean startup log on the new image digest.

Decision 38
- React-bound dependency modernization after the React 19 bump: replace `react-popper` with `@floating-ui/react` and `@floating-ui/react-dom`, and upgrade the remaining high-value runtime packages that have current React 19 support, specifically `react-redux` to `9.2.0`, `react-focus-lock` to `2.13.7`, `react-slider` to `2.0.6`, and `react-tabs` to `6.1.0`.
- Rationale: these packages are on the critical menu/tooltip/select/state path, so modernizing them removes the old popper stack, aligns Redux bindings with current React, and reduces the remaining React-era compatibility surface without waiting for a larger router migration.

Decision 39
- Post-modernization validation and promotion: keep the upgraded dependency set, push `192.168.1.5:15000/readarr:latest` at digest `sha256:75f5f0fe5456418d61d6a09f07ea18be2a2ec0d6b5990450f3aed4ddc517037f`, and redeploy it to `192.168.1.4` after the Docker image build succeeds and production verification passes with `initialize.json`, an EPUB download `curl`, and startup/download log checks.
- Rationale: the new image built successfully, the live host pulled the matching digest, `initialize.json` still served `10.0.0.38214`, the EPUB endpoint returned `HTTP/1.1 200 OK`, and production logs showed clean startup. Remaining ecosystem lag is now concentrated in `connected-react-router`, `react-dnd-multi-backend`, and `react-lazyload`, which still merit follow-up but did not block this validated React 19 deployment.

Decision 40
- Final React-era dependency cleanup: remove `connected-react-router` entirely in favor of plain `react-router-dom` with `withRouter`/`Router history`, replace `react-lazyload` with a local IntersectionObserver-based image wrapper, and upgrade `react-dnd-multi-backend` plus `rdndmb-html5-to-touch` to `9.0.0`.
- Rationale: `connected-react-router` and `react-lazyload` were the last clearly outdated runtime packages left from the React 17-era stack, and replacing them directly removes more peer-pressure and dead integration code than trying to carry them forward under React 19.

Decision 41
- Final validation and promotion for the router/lazy-image cleanup: keep the new image, push `192.168.1.5:15000/readarr:latest` at digest `sha256:d33a16f69ec1a28e8096f8503953b9e82d1da16d2b33e785e21d289c7d7b853e`, redeploy it to `192.168.1.4`, and confirm the live bundle no longer contains `connected-react-router` or `react-lazyload`.
- Rationale: `corepack yarn install --mode=skip-build`, `corepack yarn build`, and `docker build` all succeeded; the live host pulled the matching digest, still served `initialize.json` at `10.0.0.38214`, returned `HTTP/1.1 200 OK` for the EPUB download endpoint, showed a clean startup log, and bundle/source greps found no remaining `connected-react-router` or `react-lazyload` references.


Decision 42
- Author poster regression fix: explicitly pass `coverType="poster"` from `AuthorPoster` into `AuthorImage`, then promote the fix after rebuilding, pushing `192.168.1.5:15000/readarr:latest` at digest `sha256:82ac265a141654d5683057684403fc1e8fa961df39a807ca8adc4e9a1e2e2c5e`, redeploying to `192.168.1.4`, and verifying the live bundle plus normal curl/log checks.
- Rationale: author records from the live API still exposed poster images, but `AuthorPoster` was not telling `AuthorImage` to look for the `poster` cover type, so `/authors` rendered placeholders while book covers continued to work. The rebuilt prod image still served `initialize.json`, still returned `HTTP/1.1 200 OK` for the EPUB download endpoint, showed a clean startup log, and the live compiled `AuthorPoster` code now includes `coverType: "poster"`.

Decision 43
- Shared poster component fix: make `AuthorPoster` default `coverType` to `poster` but preserve any explicit caller override, then promote the corrected image after rebuilding, pushing `192.168.1.5:15000/readarr:latest` at digest `sha256:eca8eb01bced4c36ed9a4d73454d1774950808385a8672c377acb86bf9ae3a41`, redeploying to `192.168.1.4`, and verifying the live bundle plus standard curl/log checks.
- Rationale: `/books` reuses `AuthorPoster` and passes `coverType="cover"` for book art, so hard-overriding the prop to `poster` fixed `/authors` but broke `/books`. The live compiled bundle now shows `coverType = 'poster'` as a defaulted prop that is still forwarded, which preserves both author posters and book covers.

Decision 44
- Stale utility-package cleanup: remove `react-document-title`, `react-text-truncate`, and `react-custom-scrollbars-2`, replace them with local `useDocumentTitle`, `ClampText`, and a native overflow-based `OverlayScroller`, and bump `@floating-ui/react` plus `@floating-ui/react-dom` to their current compatible releases.
- Rationale: these packages were still carrying old React-era compatibility baggage after the React 19 migration, but their behavior was small enough to replace locally without a broad architectural rewrite. The updated frontend build passed, the full Docker image built successfully, a disposable local container served `initialize.json` and logged `Now listening on: http://[::]:8787`, the image was pushed as `192.168.1.5:15000/readarr:latest` with digest `sha256:15a4746e44c19d9995d25be9efbbfb2539071389bc8b3f027de208827b61d2b0`, and production on `192.168.1.4` still served `initialize.json`, returned `HTTP/1.1 200 OK` for the EPUB download endpoint, and showed a clean startup log on version `10.0.0.38223`.

Decision 45
- Router and store modernization: move Readarr from `react-router-dom` v5 and `history` v4 to `react-router-dom` v7 plus `history` v5 through a local compatibility layer, update route declarations to `Routes`/`Route element`, upgrade `redux`, `redux-thunk`, `redux-actions`, `reselect`, and `react-google-recaptcha`, and replace the jQuery-backed `createAjaxRequest` transport with a `fetch` and `AbortController` implementation that preserves the existing `.done()`/`.fail()` call pattern.
- Rationale: the remaining large modernization gaps after the React 19 upgrade were the router/history stack and the old XHR transport layer. A compatibility layer allowed the app to keep existing `withRouter`, route-prop, and `history.block()` usage while upgrading to current routing primitives, and the fetch-backed request helper removed the last direct dependency on the old jQuery AJAX path without forcing a full action-creator rewrite in one step.

Decision 46
- Validation and promotion for the router/history/fetch migration: keep the upgraded image, push `192.168.1.5:15000/readarr:latest` at digest `sha256:95cb16320dfff3e29f02dbf3faf12d4db6a4e5071b4c86c4597d03da930ed968`, redeploy it to `192.168.1.4`, and verify local and production behavior with `initialize.json`, guarded page route curls, the live EPUB download endpoint, and startup logs.
- Rationale: `corepack yarn install --mode=skip-build`, `corepack yarn build`, and the full Docker image build all succeeded after the compatibility fixes for `redux-thunk` v3, `reselect` v5, and React-Redux v9 dispatch typing. A disposable local container served `initialize.json`, production on `192.168.1.4` served `initialize.json` at `10.0.0.38214`, the EPUB download endpoint returned `HTTP/1.1 200 OK`, the guarded `/authors` and `/system/logs/files/update` routes returned the expected `401 Unauthorized` headers without crashing, and the live container log showed clean startup on backend version `10.0.0.38223`.

Decision 47
- Redux helper cleanup: remove the external `redux-actions` package and replace it with a local `Store/reduxActionsCompat` helper that provides the small `createAction` and `handleActions` surface Readarr actually uses, then switch all store action files and `createHandleActions` to that local module.
- Rationale: `redux-actions` was the remaining stale Redux-specific helper dependency spread across a large number of action files, but the codebase only relied on a narrow subset of its API. Replacing it locally removes an old package and its types from the dependency graph without forcing a full reducer/action rewrite in one pass, which is a better maintainability win than carrying the package forward.

Decision 48
- App-shell connector cleanup and promotion: convert the top-level `ConnectionLostModalConnector` and sidebar `MessagesConnector` from `connect(...)` wrappers to direct function components using props and `useAppSelector`, then promote the image after local and production verification. The validated image digest is `sha256:cb6e5c6cf9cf27aa1146ea95a562725fedc258136edb22b8ef21d645e4abe2aa`.
- Rationale: these connectors sit on central UI paths and were trivial to rewrite with hooks, so converting them proves the newer store-hook path is working while reducing connector boilerplate where it provides little value. After removing `redux-actions`, `corepack yarn install --mode=skip-build`, `corepack yarn build`, and the full Docker build all passed; the disposable local container served `initialize.json` and logged a clean startup; production on `192.168.1.4` still served `initialize.json`, the EPUB download endpoint returned `HTTP/1.1 200 OK`, the container log was clean, and the live bundle contained `Store/reduxActionsCompat` with no remaining `redux-actions` references in source, `package.json`, or `yarn.lock`.

Decision 49
- Media-cover self-heal strategy: add a housekeeping task that scans all authors and books for missing or zero-byte local cover files and re-downloads them through `IMapCoversToLocal`, while also making `EnsureBookCovers` tolerant of books whose usable cover only exists on an unmonitored edition.
- Rationale: cover downloads previously depended too heavily on refresh-time event flow, so transient fetch failures or later file loss could leave permanent gaps with no recovery path. The new housekeeping pass makes missing covers self-healing, and the book-cover selection fix removes the brittle `Single(x => x.Monitored)` assumption that prevented some valid covers from being restored at all.

Decision 50
- Validation and promotion for missing-cover repair: keep the housekeeping-based cover backfill implementation, push `192.168.1.5:15000/readarr:latest` at digest `sha256:edc24c94ce93874bbce9686c2969781a6f8d8ea2a8a37786b030de1c2d57b17b`, redeploy it to `192.168.1.4`, and verify both the normal app path and the new repair path with targeted tests, live curls, and log inspection.
- Rationale: the focused .NET test run for `MediaCoverServiceFixture` plus `RefreshMissingMediaCoversFixture` passed 20/20, the rebuilt Docker image served `initialize.json` locally with a clean startup log, production on `192.168.1.4` still served `initialize.json` (`10.0.0.30377`), the EPUB download endpoint returned `HTTP/1.1 200 OK`, and a manual `Housekeeping` API call caused live `readarr` logs to show `HousekeepingService: Running housecleaning tasks` followed by multiple `MediaCoverService: Downloading Cover ...` entries and `RefreshMissingMediaCovers: Refreshed missing media covers for 0 authors and 40 books`, confirming the new backfill loop is executing on prod.

Decision 51
- Hotfix the blank `/book/:titleSlug` page by restoring the React Router compatibility wrapper on `BookDetailsPageConnector`, then rebuild, push `192.168.1.5:15000/readarr:latest` at digest `sha256:b37a33d9d318d20f6bdd37084a69ede8cbf5be2f1427753ea5228b9e8b693d29`, redeploy to `192.168.1.4`, and verify the route-related bundle output plus normal curl/log checks.
- Rationale: after the router modernization, `BookDetailsPageConnector` still read `match.params.titleSlug` but was no longer wrapped in `withRouter`, so the page mounted without route params and blanked in the browser. The frontend rebuild succeeded, the full Docker image rebuilt successfully, the promoted prod image still served `initialize.json`, the EPUB download endpoint still returned `HTTP/1.1 200 OK`, startup logs were clean, and the live compiled bundle on `192.168.1.4` now shows `export default withRouter(connect(createMapStateToProps)(BookDetailsPageConnector));`, confirming the route param injection path is restored.

Decision 52
- Fix broken Settings and System navigation by aligning the shared internal `Link` component with Sonarr's explicit router-link branching, then rebuild, push `192.168.1.5:15000/readarr:latest` at digest `sha256:1ee7cf8fd8acfee523888cd3d6f4a173e12d22e4af8223b3750a8e369757489d`, redeploy to `192.168.1.4`, and verify startup plus live HTTP behavior.
- Rationale: after the router migration, route definitions still existed and direct guarded requests to `/settings/...` and `/system/...` returned normal `401 Unauthorized` responses, which indicated backend routing was fine and the regression was in frontend navigation. `Link.tsx` had diverged from Sonarr and still used a generic `React.createElement` path that mixed router and anchor semantics; switching it back to explicit `<RouterLink>`, `<a>`, and non-link rendering paths restored internal navigation behavior without changing the route table. The rebuilt local container started cleanly, served `initialize.json`, and logged normal startup on port `8787`; the promoted prod container on `192.168.1.4` restarted cleanly, served `initialize.json`, showed normal startup logs on version `10.0.0.30390`, and is now running the new image digest for live verification in-browser.

Decision 53
- Replace internal SPA `Link` navigation with normal anchor navigation as a stability fallback, then rebuild, push `192.168.1.5:15000/readarr:latest` at digest `sha256:a281ab9166067060acb11b46fc78ce81b29c8ee5bb75767a546ce7056129ee52`, redeploy to `192.168.1.4`, and verify startup plus live HTTP behavior.
- Rationale: user verification showed the affected Settings/System destinations worked when opened in a new tab but failed only on same-tab client-side navigation, which narrowed the regression to the in-browser route transition path rather than the destination pages or backend routes. Rendering internal links as normal anchors makes same-tab clicks use the same full-page load path that was already known-good, avoiding the remaining router-compat bug while preserving correct URLs. The rebuilt local container started cleanly, served `initialize.json`, and logged normal startup on port `8787`; the promoted prod container on `192.168.1.4` restarted cleanly, served `initialize.json`, and showed normal startup logs on version `10.0.0.30390`, so the anchor-navigation fallback is now live for browser verification.

Decision 54
- Scope the settings navigation blocker so `history.block()` is only registered while there are actual unsaved setting changes, then rebuild, push `192.168.1.5:15000/readarr:latest` at digest `sha256:8f3a5a51f9e821a5be396e28015c1d4939b25fd9bf4fb9b0fc52781327cffc4c`, redeploy to `192.168.1.4`, and verify startup plus live HTTP behavior.
- Rationale: once internal links were forced through normal anchors, the browser started showing a full-page unload prompt on every Settings click. The only active navigation guard in the frontend was `SettingsToolbarConnector`, which registered `history.block()` unconditionally at mount time. Registering and unregistering that blocker based on `hasPendingChanges` preserves the intended dirty-form protection while eliminating the false-positive browser prompt during ordinary settings browsing. The rebuilt local container started cleanly, served `initialize.json`, and logged normal startup on port `8787`; the promoted prod container on `192.168.1.4` restarted cleanly, served `initialize.json`, and the live compiled bundle now contains `updateNavigationBlocker`, confirming the scoped blocker logic is what production is running.

Decision 55
- Restore proper internal `RouterLink` SPA navigation while keeping the scoped settings blocker fix, then rebuild, push `192.168.1.5:15000/readarr:latest` at digest `sha256:6ee8b3704f0494b595f3267076d0649089b2b0a83b183382f63088c4b2c59511`, redeploy to `192.168.1.4`, and verify with real browser probes plus the usual curl and log checks.
- Rationale: the anchor-based fallback made the app usable again but forced a full page reload on every internal click. With `SettingsToolbarConnector` no longer installing a false-positive navigation blocker, the next step is to put internal links back on `RouterLink` and validate the real browser behavior instead of guessing from source. The refreshed source build succeeded, the rebuilt local image starts cleanly, serves `initialize.json`, and logs normal startup; after promotion, production on `192.168.1.4` restarted cleanly, served `initialize.json`, and a Playwright probe against the live UI changed the URL from `/` to `/books` with `navigationResponseStatus: null`, `navigationRequests: []`, and no new `performance` navigation entry, confirming in-app SPA routing is restored without a full document reload.

Decision 56
- Torznab indexer modal crash fix: make `HintedSelectInputSelectedValue` tolerate undefined multi-select values and missing option lists by normalizing them to empty arrays before rendering labels.
- Rationale: the add-indexer flow can hand the selected-value component `isMultiSelect=true` before the backing field has initialized `value`, which caused `value.map(...)` to throw and blank the modal. Normalizing the render path fixes the crash at the exact failing component without changing the saved provider payload shape.

Decision 57
- Promotion for the Torznab modal crash fix: push `192.168.1.5:15000/readarr:latest` at digest `sha256:cfb1c351090c55d3a339065e32b1315ac60eb7a2e6536268eb09d6e1215bf23e`, recreate `readarr` on `192.168.1.4`, and verify the live service with `curl` plus container logs.
- Rationale: `AGENTS.md` requires every tested change to be promoted through the local registry and deployed after validation. The frontend build succeeded, the disposable local container started cleanly, `initialize.json` returned `HTTP/1.1 200 OK`, the live TrueNAS host pulled the new image, `docker compose up -d --force-recreate readarr` replaced the service, `http://127.0.0.1:8787/initialize.json` on `192.168.1.4` returned `HTTP/1.1 200 OK`, and the running `readarr` logs show normal startup on version `10.0.0.30390` listening on port `8787`.

Decision 58
- Floating select fix: pass Floating UI the actual DOM node from `Scroller` via `registerScroller={refs.setFloating}` instead of attaching `refs.setFloating` as a React `ref` to the `Scroller` class component.
- Rationale: `EnhancedSelectInput` renders its dropdown through `Scroller`, which is a class component wrapping an internal `<div>`. React gives refs on class components the component instance, not the DOM element, so Floating UI received an object with no `ownerDocument`, causing the `defaultView` crash when opening the Torznab modal. Wiring `refs.setFloating` to `Scroller`'s DOM callback restores the expected element contract.

Decision 59
- Promotion for the floating select fix: push `192.168.1.5:15000/readarr:latest` at digest `sha256:aa92d19f96d7e74681d36b471d225e9392e9ecd898600779edabc641f290ba0c`, recreate `readarr` on `192.168.1.4`, and verify the live service again with `curl` plus logs.
- Rationale: the floating-node fix changed runtime UI behavior, so it needed the same full promotion path as the earlier null-guard fix. The rebuilt disposable container started cleanly and served `initialize.json` with `HTTP/1.1 200 OK`; the image was then pushed to the local registry, the TrueNAS host pulled and recreated `readarr`, `http://127.0.0.1:8787/initialize.json` again returned `HTTP/1.1 200 OK`, and the live logs show normal startup on version `10.0.0.30390` listening on port `8787`.

Decision 60
- Harden dynamic select handling in `EnhancedSelectInput`: normalize missing `values` to `[]`, normalize multi-select values to arrays, and guard keyboard/open selection logic when the option list is still empty.
- Rationale: the Torznab categories field is a dynamic multi-select loaded from `newznabCategories`, so it can briefly have `null`/empty selected values and no option list while the modal opens and fetches categories. Making the shared select component resilient to that transient state avoids category-dropdown crashes even when provider actions return slowly or the field starts unset.

Decision 61
- Promotion for the dynamic-select hardening: push `192.168.1.5:15000/readarr:latest` at digest `sha256:60bf49ab8bc58b8c18c62f3df3f534b12bdbaaa10b9b9b78c641fb4f03ea4cfa`, recreate `readarr` on `192.168.1.4`, and verify the live service with `curl` plus logs.
- Rationale: the hardening touched shared select behavior, so it needed the full test/promote path again. The rebuilt disposable container started cleanly and served `initialize.json` with `HTTP/1.1 200 OK`; the image was pushed to the local registry, the TrueNAS host pulled and recreated `readarr`, `http://127.0.0.1:8787/initialize.json` returned `HTTP/1.1 200 OK`, and the live logs show normal startup on version `10.0.0.30390` listening on port `8787`.

Decision 62
- Explicit multi-select support for provider category fields: mark `newznabCategories` fields named `categories` as multi-select in `ProviderFieldFormGroup`, and normalize non-array stored values in `EnhancedSelectInput` so blank, null, scalar, and legacy comma-separated category values all behave like proper arrays.
- Rationale: the categories dropdown was still unstable because the shared select control inferred single vs multi-select solely from the current saved `value`. That breaks when a categories field starts unset or comes back from older data as a string, which makes the UI treat it as a single select and mis-handle selection. Explicitly marking the field and normalizing the value shape fixes the categories control at the contract level instead of relying on incidental array values.

Decision 63
- Promotion for the explicit categories multi-select fix: push `192.168.1.5:15000/readarr:latest` at digest `sha256:88a3bda0e8fb45079be9fa49e35524124954426e343d2c93f578b674845b6e19`, recreate `readarr` on `192.168.1.4`, and verify the live service with `curl` plus logs.
- Rationale: the categories contract fix changes shared field rendering, so it required the full test/promote cycle again. The rebuilt disposable container started cleanly and served `initialize.json` with `HTTP/1.1 200 OK`; the image was pushed to the local registry, the TrueNAS host pulled and recreated `readarr`, `http://127.0.0.1:8787/initialize.json` returned `HTTP/1.1 200 OK`, and the live logs show normal startup on version `10.0.0.30390` listening on port `8787`.

Decision 64
- Torznab recent-feed validation fix: change `NewznabRequestGenerator.GetRecentRequests()` to use `t=book` only when the indexer supports a real structured book search, and otherwise fall back to `t=search`; add a Torznab unit test that pins the Jackett/TPB case where `book-search` advertises only `q`.
- Rationale: Readarr was validating Torznab indexers by issuing `t=book` whenever the feed exposed any `book-search` capability at all. Jackett's TPB Torznab feed reports `book-search q` in caps, which made Readarr validate against the wrong endpoint and reject the indexer with `Query successful, but no results in the configured categories were returned...` even though normal `t=search` requests against the same categories returned results. The focused .NET test run in a `mcr.microsoft.com/dotnet/sdk:10.0` container passed `TorznabFixture` 10/10, including the new assertion that recent-feed validation now calls `t=search` instead of `t=book` for that capability shape.

Decision 65
- Promote and apply the Torznab validation fix: build and push `192.168.1.5:15000/readarr:latest` at digest `sha256:1e37beb18686f87a38e59438869e76ab29ad5034c2568234e06d6da5cf36f7ad`, redeploy `readarr` on `192.168.1.4`, and create the live `TPB Jackett` Torznab indexer pointing at `http://192.168.1.4:9117/api/v2.0/indexers/thepiratebay/results/torznab/api` with categories `[100601, 100102]`.
- Rationale: `corepack yarn build` succeeded, the rebuilt Docker image started cleanly in a disposable local container, `curl http://127.0.0.1:18793/initialize.json` returned `HTTP/1.1 200 OK`, and container logs showed normal startup on port `8787`. The exact failing user flow was then verified against that disposable container: `POST /api/v1/indexer` for the TPB Jackett Torznab payload returned `HTTP/1.1 201 Created`. After promotion and redeploy, `http://127.0.0.1:8787/initialize.json` on `192.168.1.4` returned `HTTP/1.1 200 OK` with version `10.0.0.17350`, `docker logs --tail 80 readarr` showed clean startup on backend version `10.0.0.17370`, the same live `POST /api/v1/indexer` returned `HTTP/1.1 201 Created`, and `GET /api/v1/indexer` now shows `TPB Jackett` as indexer `id: 2` with the expected Jackett URL, API key, and category values.

Decision 66
- Confirm the deployed TPB Jackett Torznab indexer config is valid and identify the remaining 404 as a form-entry error rather than a saved-config bug.
- Rationale: `GET /api/v1/indexer/2` on `192.168.1.4` shows the saved live config uses `baseUrl=http://192.168.1.4:9117` and `apiPath=/api/v2.0/indexers/thepiratebay/results/torznab/api`, which is the correct split for Readarr's URL builder. `POST /api/v1/indexer/test` using that exact saved payload returned `HTTP/1.1 200 OK {}`. The user's reported 404 URL includes the full Jackett path in both the base and apiPath portions, which only happens when the full feed URL is pasted into the form's `URL` field while `API Path` still contains the Torznab path.

Decision 67
- Resume promotion for the multiselect selected-value rendering fix from the already-built image by rerunning the disposable-container smoke test, checking `initialize.json` and startup logs, then pushing `192.168.1.5:15000/readarr:latest` and redeploying only after that verification passes.
- Rationale: the prior session disconnected after the image build completed and while preparing a temporary smoke-test directory. The built image must still be validated against the exact artifact before promotion to satisfy `AGENTS.md`, and writing this checkpoint preserves the recovery point before the docker-only verification and deployment steps.

Decision 68
- Complete promotion for the multiselect selected-value rendering fix by publishing `192.168.1.5:15000/readarr:latest` at digest `sha256:e187ce23d54ec362f2c249fbdeaa10b52a08ad2eb226f1b3fe65556b41856dc9`, recreating the `readarr` service from `/mnt/tank/media/multimedia/compose.yaml` on `192.168.1.4`, and verifying both the disposable container and live service with `initialize.json` plus startup logs.
- Rationale: the disposable local container from the rebuilt image served `HTTP/1.1 200 OK` on `http://127.0.0.1:18793/initialize.json` with version `10.0.0.17350`, and its logs showed normal startup through `Now listening on: http://[::]:8787` and `Application started`. After pushing that exact image digest, `docker compose pull readarr && docker compose up -d --force-recreate readarr` on `192.168.1.4` replaced the production container, `http://127.0.0.1:8787/initialize.json` returned `HTTP/1.1 200 OK` with version `10.0.0.17350`, and `docker logs --tail 120 readarr` showed clean startup on backend version `10.0.0.17370` listening on port `8787`.

Decision 69
- Normalize pasted Newznab/Torznab feed URLs in the indexer edit modal by splitting an absolute feed URL into `baseUrl`, hidden `apiPath`, and `apiKey` as soon as the `URL` provider field changes.
- Rationale: a headless Chromium repro against `http://192.168.1.4:8787/settings/indexers` showed the visible Torznab form only exposes `URL`, `API Key`, and `Categories` by default. Pasting the full Jackett feed URL into `URL` caused both `POST /api/v1/indexer/test` and `POST /api/v1/indexer` to return `400 Bad Request`, after which the modal rendered `Invalid API Key`. The root cause is that the UI left the full Jackett feed URL in `baseUrl` and never populated the hidden `apiPath` field, so the fix is to normalize that URL before the provider payload is built.

Decision 70
- Treat `item.implementation` in the indexer modal connector as a wrapped settings value and unwrap `.value` before deciding whether to normalize the pasted provider URL.
- Rationale: the first normalization patch rebuilt and deployed cleanly, but the live headless Chromium repro still posted `baseUrl=http://192.168.1.4:9117/api/v2.0/indexers/thepiratebay/results/torznab/api?...` with hidden `apiPath=/api`. Captured request bodies showed the normalizer never executed because `createProviderSettingsSelector` wraps most top-level fields as `{ value, ... }`, so `item.implementation` was an object instead of the string `Torznab`, causing the implementation gate in `normalizeIndexerUrl()` to fail open.

Decision 71
- Keep the corrected Jackett URL normalization fix live and treat the remaining Torznab add failure as a separate category-default problem: the UI now posts the correct `baseUrl`, hidden `apiPath`, and `apiKey`, but still tests and saves against the default category set `[3030, 7020, 8010]`, which returns no TPB results.
- Rationale: after rebuilding, smoke-testing, pushing `192.168.1.5:15000/readarr:latest` at digest `sha256:f3b5d6af61bfbcf90b25144d8cb94309a3cda1c0bd11a2bde0ab2e05c73d1a2d`, and redeploying `readarr` on `192.168.1.4`, the live headless Chromium repro against `http://192.168.1.4:8787/settings/indexers` changed behavior materially. The captured `POST /api/v1/indexer/test` payload now contains `baseUrl=http://192.168.1.4:9117`, `apiPath=/api/v2.0/indexers/thepiratebay/results/torznab/api`, and the correct Jackett API key instead of the raw feed URL stuffed into `baseUrl`. The live modal no longer reports `Invalid API Key`; it now reports `Query successful, but no results in the configured categories were returned from your indexer.`, which matches the posted default categories rather than the existing working TPB category pair `[100601, 100102]`.

Decision 72
- Override `TorznabSettings` default categories to Readarr's torrent-style book/audio-book pair `[100601, 100102]` and add an API schema test that asserts newly-created Torznab indexers expose those defaults.
- Rationale: the live category-options action already returned the right TPB-compatible options, including `100601` and `100102`, so the remaining failure came from the starting schema state. `TorznabSettings` was inheriting `NewznabSettings` defaults `[3030, 7020, 8010]`, which are acceptable for generic Newznab but wrong for Torznab trackers. Setting the Torznab constructor defaults fixes the add-form payload at the source instead of relying on UI remapping heuristics after the user opens the category dropdown.

Decision 73
- Downgrade the empty recent-feed result in `HttpIndexerBase.TestConnection()` from an error to a warning so valid indexers with zero matches in their recent query can still be added and saved.
- Rationale: after the URL-normalization and Torznab-default-category fixes were live, both the headless Chromium repro and direct `POST /api/v1/indexer/test` requests still returned `400` with `Query successful, but no results in the configured categories were returned from your indexer.` even when posting the same normalized TPB Jackett endpoint and `[100601,100102]` category pair used by the already-saved working indexer. The blocking behavior came from `HttpIndexerBase.TestConnection()`, which treated an empty recent feed as a fatal validation failure. Converting that specific case to `IsWarning = true` preserves visibility of the condition while allowing the indexer to be created.

Decision 74
- Stop warning-only provider test results from blocking indexer create and update flows by excluding warnings from `ProviderControllerBase` save-time validation while still preserving explicit test results in the UI.
- Rationale: after `HttpIndexerBase.TestConnection()` was changed to emit a warning for empty recent feeds, the live add flow still failed on the first save because `CreateProvider` and `UpdateProvider` were calling `GetDefinition(... includeWarnings: !forceSave ...)` and `Test(... includeWarnings: true/!forceSave)`, which rethrew warnings as API `400` responses during save. Saving only needs hard validation errors to block persistence; warning display belongs in the explicit test path and in the provider resource, not in the persistence gate.

Decision 75
- Promote the save-validation fix as `192.168.1.5:15000/readarr:latest` digest `sha256:44d19c4d929344b43e3cd91119388dc32986b49e51f6f1a624b6a9763f2a9da8`, redeploy `readarr` on `192.168.1.4`, and verify with both standard health checks and a live headless Chromium add-indexer repro using the full Jackett TPB Torznab feed URL.
- Rationale: focused backend verification passed (`Readarr.Core.Test` Torznab tests green and `Readarr.Api.V1` built successfully), the rebuilt disposable container served `HTTP/1.1 200 OK` from `http://127.0.0.1:18793/initialize.json`, and its logs showed normal startup through `Now listening on: http://[::]:8787` and `Application started`. After pushing and redeploying, `http://127.0.0.1:8787/initialize.json` on `192.168.1.4` returned `HTTP/1.1 200 OK`, live logs showed clean startup on backend `10.0.0.4226`, and the live headless browser repro posted the normalized Jackett endpoint with categories `[100601,100102]`, received the expected warning-only `POST /api/v1/indexer/test` response, then completed `POST /api/v1/indexer` with `HTTP/1.1 201 Created` on the first save attempt. The temporary repro indexer was then deleted from the live instance to leave production clean.

Decision 76
- Preserve the full pasted Jackett Torznab feed URL in the visible `URL` field by moving Newznab/Torznab URL normalization out of the modal's `onFieldChange` path and into `getProviderState()` so only the outbound save/test payload is split into `baseUrl`, `apiPath`, and `apiKey`.
- Rationale: the previous frontend patch fixed the payload normalization but still rewrote the visible `URL` field down to the origin (`http://192.168.1.4:9117`) as soon as the user pasted the full feed URL, which made the UI look truncated and misleading. After rebuilding, smoke-testing, pushing `192.168.1.5:15000/readarr:latest` at digest `sha256:069ef52aa44f376846d3e1f184e83452f02344a43ed1af5825ba0ecdec1470da`, and redeploying `readarr` on `192.168.1.4`, a live headless Chromium repro confirmed the modal now keeps `http://192.168.1.4:9117/api/v2.0/indexers/thepiratebay/results/torznab/api?apikey=...` in the visible `URL` input before save while the emitted `POST /api/v1/indexer/test` and `POST /api/v1/indexer` payloads still normalize to `baseUrl=http://192.168.1.4:9117`, `apiPath=/api/v2.0/indexers/thepiratebay/results/torznab/api`, and the Jackett API key. The warning-only test response remains expected for this TPB feed, the save still returned `HTTP/1.1 201 Created`, and the temporary live repro indexer was deleted afterward.

Decision 77
- Fix the remaining live Torznab-TPB defects by reconstructing the full saved display URL for reopened Torznab entries in the frontend selector and by treating Torznab's empty recent-feed result as a successful test connection instead of a warning response.
- Rationale: direct headless verification of the existing live `Torznab-TPB` indexer showed the saved edit modal still opened with only `http://192.168.1.4:9117` in the visible `URL` field and `POST /api/v1/indexer/test` still returned the warning body even though `Save` succeeded. The frontend selector fix now rebuilds the displayed URL from stored `baseUrl`, `apiPath`, and `apiKey` when existing Torznab settings are loaded, including Jackett's `/torznab/` path shape. The backend Torznab override now swallows the empty-feed warning from `HttpIndexerBase.TestConnection()` so a reachable Torznab endpoint tests cleanly. After rebuilding, smoke-testing, pushing `192.168.1.5:15000/readarr:latest` at digest `sha256:530f72e26a77e9a45ed07ae27edbcb40915abd3354875a11b3c31e5d39db7e3d`, and redeploying `readarr` on `192.168.1.4`, a live headless Chromium check of the saved `Torznab-TPB` entry showed the modal reopening with `http://192.168.1.4:9117/api/v2.0/indexers/thepiratebay/results/torznab/api?apikey=...` in the `URL` input, `POST /api/v1/indexer/test` returning `HTTP/1.1 200 OK {}`, and `Save` still returning `HTTP/1.1 202 Accepted`. Jackett logs captured during the same window showed the actual TPB Torznab searches reaching Jackett and returning `Found 0 releases (from cache)`, confirming the successful test result is based on a real upstream request rather than a UI-only change.

Decision 78
- Secure Jackett with an admin password, then bulk-enable and test public indexers through Jackett's authenticated UI API instead of brittle modal clicks.
- Rationale: the live Jackett instance was still exposing the `Security Risk: Your instance has external access enabled without using an admin password.` warning. The password was set to `M@ster021` via Jackett's own in-page `api.updateAdminPassword(...)` call, which updated `/mnt/tank/media/config/jackett/Jackett/ServerConfig.json` with the hashed password and switched unauthenticated access to the login page. With authenticated headless browser access working, a purpose-built bulk script enabled and tested all 111 public Jackett indexers. That first pass identified 57 trackers that passed Jackett's own test path and 54 that failed; all 54 failures were then deleted back out of Jackett so the configured set only contained the passing candidates.

Decision 79
- Import only the Jackett trackers that also behave plausibly for Readarr book searches, and remove category-incompatible or live-error trackers from both Jackett and Readarr until the configured sets match.
- Rationale: the authenticated bulk import into Readarr used the live TPB Torznab payload shape (`baseUrl=http://192.168.1.4:9117`, `apiPath=/api/v2.0/indexers/{id}/results/torznab/`, shared Jackett API key, and categories `[3000,7000]`) and all 57 Jackett-pass candidates also passed `POST /api/v1/indexer/test` and saved successfully. Runtime verification against live `readarr` and `jackett` logs then exposed two additional non-working subsets: 32 trackers logging `All categories provided are unsupported` for Readarr's configured categories, and 4 more trackers (`AniRena`, `Bangumi Moe`, `ExtraTorrent.st`, `MixtapeTorrent`) that still produced live Torznab HTTP failures or timeouts on actual searches. Those 36 trackers were deleted from both systems. Final verification on `192.168.1.4` shows `curl -D - http://127.0.0.1:8787/initialize.json` returning `HTTP/1.1 200 OK`, Readarr now holding 21 Jackett Torznab indexers (22 total indexers including the existing Newznab entry), and Jackett holding the same 21 configured public trackers: `1337x`, `arabtorrents-com`, `bigfangroup`, `bitru`, `bitsearch`, `btetree`, `demonoid-clone`, `dmhy`, `epublibre`, `internetarchive`, `nipponsei`, `noname-club`, `nortorrent`, `nyaasi`, `therarbg`, `tokyotosho`, `torrentdownload`, `torrentdownloads`, `torrentgalaxyclone`, `zamundarip`, and `zktorrent`. Recent logs still show some in-flight searches from the pre-cleanup 57-indexer window, but the latest configured-count checks on both services have converged to the 21-indexer final state.
